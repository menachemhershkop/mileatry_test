export class launcher {
   constructor(id, name, rocketType,latitude, longitude, city){
    this.id=id,
    this.name= name,
    this.rocketType=rocketType,
    this.latitude = latitude,
    this.longitude = longitude,
    this.city = city
   }
}